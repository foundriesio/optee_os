Docs:
=====

APDU Specification
https://www.nxp.com/docs/en/application-note/AN12413-SE050_APDU_specification.pdf

User Guidelines:
https://www.nxp.com/docs/en/user-guide/AN12514.pdf

Stack Documentation and original code:
- untar SE050-PLUG-TRUST-MW.zip
- open simw-top/doc/index.html

Port Caveats:
=============
1. One single global session and One single global key store.
2. No support for OP-TEE  multisession/multithread
3. Policies to control the Secure Objects are not enabled.
4. Transport lock?

- Two persistent Secure Object types (deletion controlled by SKS only)
    RSA
    ECC

  Deletion caveat: while running xtests, RSA/ECC keys are created but
  never deleted. During session init, we run a script to delete _ALL_
  keys (not suitable for production).

  All other elements transient (in SE050 DDR)

- Communication with SE050 over I2C and two distinct drivers
    * mxc_i2c: op-tee native. hardcoded to work on a 25MHZ clock and I2C3
    * tee-i2c: trampoline that calls linux to access I2C3
               requires kernel patch.

    The SE050 stack starts using the mxc_i2c required to access hmac
    and then transitions to tee-i2c as soon as the normal world is
    booted so the SE050 can be used with pcks-11 without bus
    collisions

    All communication between the host and the SE050 are encrypted
    using the default keys in the SE050.
    
The SE050 can support one-shot crypto operations (something that
op-tee does not yet). The SE050 interface for those is not used.

I2C drivers
===========
The native I2C driver has only been tested on the IMX8MM-LPDDR4
EVK. For simplicity - since op-tee boots directly after ROM,  the bus
clock is hardcoded to 24MHz instead of polling the hardware

The trampoline driver requires no configuration.

To select the bus used for the SE050 use the following build
configuration option (set to I2C3 (0x2) by default)
  - CFG_CORE_SE05X_I2C_BUS

The pictures in the images/ folder show SE050 connection to an
IMX8MM-LPDDR4 on I2C3.
Note that some of the cables coming out of the SE050 are routed for
the Logic Analyzer (they are probes on the SE050 i2c bus).

Persistent Key Deletion
=======================
Only supported on pkcs11 (sks). There is code in context.c to remove
any keys created during testing.

External keys (ie those not generated by the SE050) used in
operations, are not stored in the SE050.

Operation
=========
Some of the crypto operations, even though they should work as the
code is generic, they do not (ie hmac384, des3 ..); having run op-tee's xtest
as a validation, those tests that did not pass have been delegated to
libtomcrypt (hence the pick-and-poke done in the makefiles).

Regression tests
================
The crypto subset of xtests were used to validate the integration in
op-tee. It is available here [2].

Once built and installed (TA's are installed in /usr/lib/optee_armtz)
just execute as:
$ sudo xtest

Note
====
To help with a visual understanding and navegation of op-tee please
have a look at the diagrams/ folder

CONFIGS:
=======

CFG_CORE_SE05X_I2C_BUS: I2C bus
            __CFG
    I2C1     0
    I2C2     1
    I2C3     2

CFG_CORE_SE05X_DISPLAY_INFO: displays the SE050 Applet and JCOP4
 information (shows the OEFID required to know the default keys to use
 in SCP03). Doing this requires that we reinitialize the context.

  0: does not display
  1: displays

CFG_CORE_SE05X_OEFID: the OEFID determins the keys to use when
  stablishing communications with the SE050. Once we know the OEFID, we
  no longer need to display the information and we can simply provide
  this config.
                           __CFG                              
    SE050A1_ID 0xA204        0  
    SE050A2_ID 0xA205        1
    SE050B1_ID 0xA202        2
    SE050B2_ID 0xA203        3
    SE050C1_ID 0xA200        4 
    SE050C2_ID 0xA201        5
    SE050DV_ID 0xA1F4        6
